---
import Layout from "../layouts/Layout.astro";
import { ImageCarousel } from "../components/ImageCarousel";

import { loadImages } from "../util/images";

const images = await loadImages(1000);

console.log("xx", images);
---

<Layout>
  <div class="col-span-2 h-full overflow-hidden pb-16 md:pb-2 lg:col-start-2">
    <ImageCarousel images={images} client:load />
  </div>
</Layout>
